import { Route } from "@angular/router";
import { AppAuthComponent } from "./app-auth/feature/app-auth.component";

export const appAuthRoutes: Route[] = [
    { path: "", component: AppAuthComponent },
];
