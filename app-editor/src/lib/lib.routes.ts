import { Route } from "@angular/router";
import { AppEditorComponent } from "./app-editor/app-editor.component";

export const appEditorRoutes: Route[] = [
    { path: "", component: AppEditorComponent },
];
